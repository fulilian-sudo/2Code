<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.wqw.mapper.ProductMapper">

	<insert id="save">
		insert into `data`.`product`(
		`productNum`,
		`productName`,
		`cityName`,
		`departureTime`,
		`productPrice`,
		`productDesc`,
		`productStatus`)
		VALUES (
		    #{productNum},
			#{productName},
			#{cityName},
			#{departureTime},
			#{productPrice},
			#{productDesc},
			#{productStatus}
			   )

	</insert>
	<update id="edit">
		UPDATE `data`.`product` SET
			`productNum`=#{productNum},
			`productName`=#{productName},
			`cityName`=#{cityName},
			`departureTime`=#{departureTime},
			`productPrice`=#{productPrice},
			`productDesc`=#{productDesc},
			`productStatus`=#{productStatus}
		WHERE `id`=#{id};
	</update>
	<!--查询所有-->
	<select id="findAll" resultType="cn.wqw.pojo.Product">
		select *from product
	</select>
	<select id="deleteById">
		delete from product where id=#{id}
	</select>
	<select id="editById" resultType="cn.wqw.pojo.Product">
		select *from product where id=#{id}
	</select>



</mapper>